---
phase: 03-theme-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/articles/app.py
  - src/articles/theme.py
  - src/articles/extractor.py
  - tests/test_app.py
  - tests/test_extractor.py
autonomous: true
requirements: [THEME-01, THEME-02, THEME-03, PERF-01]

must_haves:
  truths:
    - "TUI renders with Catppuccin Mocha colors instead of custom cappuccino palette"
    - "No custom theme code remains in codebase (theme.py deleted)"
    - "Scrollbar is not visible during article reading"
    - "App import chain completes significantly faster with lazy trafilatura import"
  artifacts:
    - path: "src/articles/app.py"
      provides: "Catppuccin Mocha theme activation, scrollbar-free CSS"
      contains: 'self.theme = "catppuccin-mocha"'
    - path: "src/articles/extractor.py"
      provides: "Lazy trafilatura import inside extract_url()"
      contains: "import trafilatura"
    - path: "tests/test_app.py"
      provides: "Theme verification test for catppuccin-mocha"
      contains: "catppuccin-mocha"
    - path: "tests/test_extractor.py"
      provides: "Updated mock targets for lazy import"
      contains: 'patch("trafilatura.'
  key_links:
    - from: "src/articles/app.py"
      to: "textual built-in themes"
      via: "self.theme = catppuccin-mocha"
      pattern: 'self\\.theme\\s*=\\s*"catppuccin-mocha"'
    - from: "src/articles/extractor.py"
      to: "trafilatura"
      via: "lazy import inside function body"
      pattern: "def extract_url.*\\n.*import trafilatura"
    - from: "src/articles/app.py"
      to: "src/articles/extractor.py"
      via: "import extract_url (no transitive trafilatura load)"
      pattern: "from articles.extractor import extract_url"
---

<objective>
Switch the TUI from a custom cappuccino theme to the official Catppuccin Mocha built-in theme, remove all custom theme code, and optimize startup performance by lazy-importing trafilatura.

Purpose: Establish the polished visual baseline (Catppuccin Mocha is a widely recognized palette) and eliminate the ~500ms+ import delay from trafilatura that blocks app startup.

Output: Clean codebase with built-in theme, deleted theme.py, and fast-loading CLI entry point.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-theme-performance/03-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/articles/app.py (current state — to be modified):
```python
from articles.theme import CAPPUCCINO  # LINE TO REMOVE

class ArticlesApp(App):
    CSS = """
    #content {
        scrollbar-size: 0 0;  # KEEP — already hides scrollbar
    }
    """

    def __init__(self, url: str | None = None):
        super().__init__()
        self.register_theme(CAPPUCCINO)  # LINE TO REMOVE
        self.theme = "cappuccino"        # CHANGE TO "catppuccin-mocha"
        self._initial_url = url
```

From src/articles/extractor.py (current state — to be modified):
```python
import trafilatura  # LINE 3 — MOVE INSIDE extract_url()

def extract_url(url: str) -> str:
    # uses trafilatura.fetch_url() and trafilatura.extract()
```

From tests/test_app.py (current state — to be modified):
```python
async def test_app_has_cappuccino_theme():
    app = ArticlesApp()
    async with app.run_test() as pilot:
        assert pilot.app.theme == "cappuccino"  # CHANGE TO "catppuccin-mocha"
```

From tests/test_extractor.py (current state — mock targets to update):
```python
with patch("articles.extractor.trafilatura.fetch_url", ...):  # CHANGE TARGET
    with patch("articles.extractor.trafilatura.extract", ...):  # CHANGE TARGET
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Switch to Catppuccin Mocha theme and delete custom theme</name>
  <files>src/articles/app.py, src/articles/theme.py, tests/test_app.py</files>
  <read_first>
    - src/articles/app.py (current theme registration in __init__, CSS with scrollbar-size: 0 0)
    - src/articles/theme.py (entire file — will be deleted)
    - tests/test_app.py (test_app_has_cappuccino_theme assertion to update)
  </read_first>
  <behavior>
    - test_app_has_cappuccino_theme → rename to test_app_has_catppuccin_mocha_theme, assert pilot.app.theme == "catppuccin-mocha"
    - test_app_shows_welcome_without_url → still passes (no theme dependency)
    - test_app_loads_article_with_initial_url → still passes (no theme dependency)
  </behavior>
  <action>
    **Step 1: Update tests/test_app.py (RED)**
    - Rename `test_app_has_cappuccino_theme` to `test_app_has_catppuccin_mocha_theme`
    - Change assertion from `assert pilot.app.theme == "cappuccino"` to `assert pilot.app.theme == "catppuccin-mocha"`
    - Run tests — the renamed test MUST fail (app still uses cappuccino)

    **Step 2: Update src/articles/app.py (GREEN)**
    - Remove the import line: `from articles.theme import CAPPUCCINO`
    - In `__init__`, remove the line: `self.register_theme(CAPPUCCINO)`
    - In `__init__`, change: `self.theme = "cappuccino"` → `self.theme = "catppuccin-mocha"`
    - The `"catppuccin-mocha"` is a built-in Textual theme — no registration needed
    - KEEP all CSS unchanged — especially `scrollbar-size: 0 0` in `#content` (THEME-03)
    - KEEP all other imports, the rest of the class, BINDINGS, etc. unchanged

    **Step 3: Delete src/articles/theme.py**
    - Delete the entire file (it defined the CAPPUCCINO Theme constant)
    - This satisfies THEME-02: no custom theme code remains

    **Step 4: Run all tests (GREEN)**
    - All 8 tests must pass including the renamed theme test
  </action>
  <verify>
    <automated>cd /Users/berndplontsch/PARAlocal/vibes/articles && source venv/bin/activate && python -m pytest tests/ -v</automated>
  </verify>
  <acceptance_criteria>
    - src/articles/app.py contains `self.theme = "catppuccin-mocha"`
    - src/articles/app.py does NOT contain `from articles.theme import CAPPUCCINO`
    - src/articles/app.py does NOT contain `self.register_theme(`
    - src/articles/app.py contains `scrollbar-size: 0 0` (scrollbar still hidden)
    - src/articles/theme.py does NOT exist (file deleted)
    - tests/test_app.py contains `def test_app_has_catppuccin_mocha_theme`
    - tests/test_app.py contains `assert pilot.app.theme == "catppuccin-mocha"`
    - `python -m pytest tests/test_app.py -v` exits 0 with 3 tests passing
  </acceptance_criteria>
  <done>App uses built-in Catppuccin Mocha theme, custom theme.py deleted, scrollbar remains hidden, all 3 app tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy-import trafilatura for fast startup</name>
  <files>src/articles/extractor.py, tests/test_extractor.py</files>
  <read_first>
    - src/articles/extractor.py (top-level `import trafilatura` on line 3, used in extract_url body)
    - tests/test_extractor.py (mock targets `articles.extractor.trafilatura.fetch_url` and `articles.extractor.trafilatura.extract`)
  </read_first>
  <behavior>
    - All 5 existing extractor tests still pass with updated mock targets
    - extract_url("https://example.com") with mocked trafilatura returns markdown (same behavior)
    - extract_url("") still raises ValueError
    - extract_url with fetch failure still raises RuntimeError
    - Importing `articles.extractor` does NOT import trafilatura (lazy)
  </behavior>
  <action>
    **Step 1: Update tests/test_extractor.py mock targets (RED)**
    - Change ALL occurrences of `patch("articles.extractor.trafilatura.fetch_url"` to `patch("trafilatura.fetch_url"`
    - Change ALL occurrences of `patch("articles.extractor.trafilatura.extract"` to `patch("trafilatura.extract"`
    - This is required because after lazy import, `trafilatura` is no longer a module-level attribute of `articles.extractor` — it's a local variable inside `extract_url()`
    - Patching `trafilatura.fetch_url` directly works because `import trafilatura` inside the function binds the same module object from `sys.modules`
    - Run tests — they should STILL PASS at this point (the mock targets are equivalent while trafilatura is a top-level import), but this prepares for the lazy import change

    **Step 2: Move trafilatura import inside extract_url() (GREEN)**
    - In `src/articles/extractor.py`, remove the top-level `import trafilatura` (line 3)
    - Add `import trafilatura` as the FIRST line inside the `extract_url()` function body (after the docstring, before the `if not url:` check)
    - The function body becomes:
      ```python
      def extract_url(url: str) -> str:
          """...(existing docstring)..."""
          import trafilatura

          if not url:
              raise ValueError("URL must not be empty")
          ...rest unchanged...
      ```
    - Run all 8 tests — all must pass

    **Step 3: Verify startup improvement**
    - Run: `python -c "import time; s=time.perf_counter(); from articles.cli import main; print(f'{(time.perf_counter()-s)*1000:.0f}ms')"`
    - The import time should be significantly less than the ~1045ms baseline (trafilatura's ~518ms is no longer loaded at import time)
    - Note: Textual's own imports (~600ms cold) may still keep total above 500ms on cold start — this is expected and documented. The lazy import eliminates the trafilatura bottleneck which is what PERF-01 targets.
  </action>
  <verify>
    <automated>cd /Users/berndplontsch/PARAlocal/vibes/articles && source venv/bin/activate && python -m pytest tests/ -v && python -c "import time; s=time.perf_counter(); from articles.cli import main; print(f'Startup import: {(time.perf_counter()-s)*1000:.0f}ms')"</automated>
  </verify>
  <acceptance_criteria>
    - src/articles/extractor.py does NOT contain `import trafilatura` before `def extract_url`
    - src/articles/extractor.py contains `import trafilatura` INSIDE the extract_url function body (indented under def)
    - tests/test_extractor.py contains `patch("trafilatura.fetch_url"` (NOT `articles.extractor.trafilatura.fetch_url`)
    - tests/test_extractor.py contains `patch("trafilatura.extract"` (NOT `articles.extractor.trafilatura.extract`)
    - `python -m pytest tests/ -v` exits 0 with all 8 tests passing
    - `python -c "from articles.extractor import extract_url"` completes without importing trafilatura (can verify: `python -c "from articles.extractor import extract_url; import sys; assert 'trafilatura' not in sys.modules"`)
  </acceptance_criteria>
  <done>trafilatura is lazy-imported inside extract_url() only when called, startup import chain no longer loads trafilatura, all 8 tests pass with updated mock targets</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **All tests pass:** `python -m pytest tests/ -v` → 8 tests pass
2. **Theme switched:** `grep -c "catppuccin-mocha" src/articles/app.py` → 1
3. **Custom theme gone:** `test ! -f src/articles/theme.py && echo "DELETED"` → DELETED
4. **No cappuccino references:** `grep -r "cappuccino\|CAPPUCCINO" src/articles/` → no matches
5. **Scrollbar hidden:** `grep "scrollbar-size: 0 0" src/articles/app.py` → match found
6. **Lazy import:** `python -c "from articles.extractor import extract_url; import sys; assert 'trafilatura' not in sys.modules; print('LAZY OK')"` → LAZY OK
7. **Startup time:** `python -c "import time; s=time.perf_counter(); from articles.cli import main; e=time.perf_counter(); print(f'{(e-s)*1000:.0f}ms')"` → measurably faster than 1045ms baseline
</verification>

<success_criteria>
- Built-in Catppuccin Mocha theme active (THEME-01)
- theme.py deleted, no custom theme code remains (THEME-02)
- Scrollbar CSS preserved: scrollbar-size: 0 0 (THEME-03)
- trafilatura lazy-imported, not loaded at startup (PERF-01)
- All 8 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-theme-performance/03-01-SUMMARY.md`
</output>
